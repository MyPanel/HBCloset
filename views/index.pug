<!DOCTYPE html>
html(lang="en")
    head
        link(rel="stylesheet", href="css/main.css")
        meta(charset="utf-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title HB Closet
        script(src="http://code.jquery.com/jquery-latest.js")
    body
        nav(class="navbar")
            ul(class="nav-container")
                li(class="nav-item")
                    a(href="/") Home
                li(class="nav-item") 
                    a(href="/closet") Closet
                li(class="nav-item")
                    a(href="/chat") Chat
                li(class="nav-item")
                    a(href="/board") Board
                -if(currentUser)
                    li(class="nav-item")
                        a(id="username",href="/")=currentUser.username
                    li(class="nav-item")
                        a(href="/login/logout") Logout
                -else
                    li(class="nav-item")
                        a(href="/login") Login
                    li(class="nav-item")
                        a(href="/signup") Signup

        article
            block content